import Functions
print(Functions.say_hi())                       # imported the file Functions  and using its functions

EF= open("py.txt",'r')                          # reading a text file in same location and closing it.
EF.close()

import math                                     # importing python modules

#pip install pydocx   IN CMD                    # installing outside modules which are not pre installed

import docx                                     # import docx files which were installed by pip

